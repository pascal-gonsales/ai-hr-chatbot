import DemoChatInterface from '@/components/demo/DemoChatInterface'

export const metadata = {
  title: 'TeamChat AI — Demo Chat',
  description: 'Public read-only demo of the TeamChat AI HR chatbot. Fictional employee, no production data.',
}

export default function DemoChatPage() {
  return <DemoChatInterface />
}
